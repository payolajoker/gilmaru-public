# Point Packs

This directory stores public-interest point packs for Gilmaru.

- keep each pack as JSON
- use the schema in `schemas/point-pack.schema.json`
- validate with `npm run validate:data`
- explain provenance and verification in the pull request
